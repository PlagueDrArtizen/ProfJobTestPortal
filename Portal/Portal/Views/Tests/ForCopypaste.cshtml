@model IEnumerable<Portal.DataModels.QuestionInformation>

@{
    ViewBag.Title = "ForCopypaste";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ForCopypaste</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsRightAnswer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserAnswer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RightAnswer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsRightAnswer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserAnswer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RightAnswer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
